/*
import { registerPlugin } from '@capacitor/core';

import { BluetoothLe } from '../../src/definitions';

const Camera = registerPlugin<BluetoothLe>('Camera', {
  web: () => import('./web').then(m => new m.CameraWeb()),
});
*/

export * from './plugin';